<%- include(".partials/head", {link: "", titulo: "Resultado producto"})  %>
    <body>
        <%- include('partials/header.ejs') %>
        <h1>Product List</h1>
        <ul>
            <% for (let i = 0; i < productResult.length; i++) { %>
                <li>
                    <a href="/detalle/<%=productResult[i].id%>"><%= productResult[i].name %></a>
                </li>
            <% } %>
        </ul>
        <h2>Search</h2>
        <form action="/products/search" method="GET">
            <input type="text" name="search">
            <input type="submit" value="search">
        </form>
    </body>
</html>
